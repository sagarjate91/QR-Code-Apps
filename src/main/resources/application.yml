spring:
  application:
    name: QR-Code-Apps

  datasource:
    url: jdbc:mysql://localhost:3306/qr_code_app
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

